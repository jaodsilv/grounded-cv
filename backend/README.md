# GroundedCV Backend

Multi-agentic AI backend for Resume, CV, and Cover Letter tailoring.

## Installation

```bash
pip install -e ".[dev]"
```

## Development

Run the development server:

```bash
uvicorn app.main:app --reload
```

Run tests:

```bash
pytest
```
